<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>What Parents Say - School Events</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/header') %>
    <div class="container py-5">
        <h1 class="mb-4"><i class="bi bi-quote me-2"></i>What Parents Say</h1>
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger"><i class="bi bi-x-circle me-2"></i><%= error %></div>
        <% } %>
        <% if (feedback && feedback.length > 0) { %>
            <div class="row">
                <% feedback.forEach(fb => { %>
                    <div class="col-md-6 col-lg-4 mb-4">
                        <div class="card h-100 shadow-sm">
                            <div class="card-body">
                                <div class="mb-2">
                                    <% if (fb.rating) { %>
                                        <% for (let i = 1; i <= 5; i++) { %>
                                            <i class="bi bi-star-fill <%= i <= fb.rating ? 'text-warning' : 'text-secondary' %>"></i>
                                        <% } %>
                                    <% } %>
                                </div>
                                <blockquote class="blockquote mb-2">
                                    <p class="mb-0"><%= fb.message %></p>
                                </blockquote>
                                <footer class="blockquote-footer">
                                    <strong><%= fb.first_name %> <%= fb.last_name %></strong>
                                    <span class="text-muted small">(<%= fb.email %>)</span><br>
                                    <small class="text-muted"><i class="bi bi-clock me-1"></i><%= new Date(fb.created_at).toLocaleString() %></small>
                                </footer>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="alert alert-info">No feedback submitted yet.</div>
        <% } %>
    </div>
    <%- include('../partials/footer') %>
</body>
</html> 